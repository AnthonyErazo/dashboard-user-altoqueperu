/** @type {import('postcss').Postcss} */
export default {
  plugins: {
    'postcss-preset-env': {},  // Por ejemplo, si usas `postcss-preset-env`
    // Puedes agregar más plugins según tus necesidades
  },
};